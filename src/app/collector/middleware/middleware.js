// const axios = require("axios");
// const {
//   API: {
//     BASE_URL,
//     PATHS: { AUTHORIZE },
//   },
// } = require("../../lib/config");

module.exports = {
  addAddressToSession: async (req, res, next) => {
    // const address = req.body;
    // console.log("\nposties");
    // console.log(address);
    next();
  },
};
